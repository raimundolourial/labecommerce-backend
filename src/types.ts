export type users = {
    id: number,
    name: string,
    email: string,
    password: string,
    createdAt : string
    }

    export type products = {
        id: number,
        name: string,
        price: number,
        description : string,
        imageUrl : string
        }