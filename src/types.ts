export type users = {
    id: string,
    name: string,
    email: string,
    password: string,
    createdAt : string
    }

    export type products = {
        id: string,
        name: string,
        price: number,
        description : string,
        imageUrl : string
        }